import styled from 'styled-components';

const NotesContainer = styled.div`
  position: relative;

  #add-btn {
    position: absolute;
    bottom: 24px;
    right: 16px;
  }
`;

export default NotesContainer;