import styled from 'styled-components';

const NoNotes = styled.img`
  width: 105px;
  object-fit: cover;
  position: absolute;
  top: 55%;
  left: 50%;
  transform: translate(-50%, -55%);
`;

export default NoNotes;
